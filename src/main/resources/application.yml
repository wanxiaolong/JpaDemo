spring:
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://localhost:3306/demo?serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver
  #JPA相关配置
  jpa:
    hibernate:
      #会根据映射实体类自动创建或更新数据表
      ddl-auto: update
    #控制台打印sql语句
    show-sql: true
